# 顶层配置文件，编辑器在读取此文件后会停止向上级目录继续查找
root = true

# 匹配所有文件
[*]
# 缩进风格：使用空格
indent_style = space
# 缩进大小：2个空格
indent_size = 2
# 换行符：使用 LF (Linux/macOS 现代标准，也是跨平台协作的推荐标准)
end_of_line = lf
# 字符集：使用 UTF-8
charset = utf-8
# 文件末尾自动保留一个空行
insert_final_newline = true
# 移除行尾多余的空格
trim_trailing_whitespace = true
